agent_options:
  config:
    decorators:
      load:
      - SELECT uuid AS host_uuid FROM system_info;
      - SELECT hostname AS hostname FROM system_info;
    options:
      disable_distributed: false
      distributed_interval: 10
      distributed_plugin: tls
      distributed_tls_max_attempts: 3
      logger_tls_endpoint: /api/osquery/log
      logger_tls_period: 10
      pack_delimiter: /
  overrides:
controls:
  enable_disk_encryption: false
  ios_updates:
    deadline:
    minimum_version:
  ipados_updates:
    deadline:
    minimum_version:
  macos_settings:
    custom_settings:
    - path: "../lib/📱🔐-personal-mobile-devices/profiles/io.macadmins.config.test.json"
    - path: "../lib/📱🔐-personal-mobile-devices/profiles/pez-wifi-ext.mobileconfig"
  macos_updates:
    deadline:
    minimum_version:
  scripts:
  windows_enabled_and_configured: true
  windows_require_bitlocker_pin: false
  windows_updates:
    deadline_days:
    grace_period_days:
name: "📱🔐 Personal mobile devices"
policies:
queries:
software:
  app_store_apps:
  - app_store_id: "403858572"
  - app_store_id: "1163852619"
  - app_store_id: "431946152"
team_settings:
  features:
    enable_host_users: true
    enable_software_inventory: true
  host_expiry_settings:
    host_expiry_enabled: false
    host_expiry_window: 0
  integrations:
    conditional_access_enabled:
    google_calendar:
  secrets:
  - secret: SeGjfheu0j3ZLWdbEb31mW+WuMunMPjW
  webhook_settings:
    failing_policies_webhook:
      destination_url:
      enable_failing_policies_webhook: false
      host_batch_size: 0
      policy_ids:
    host_status_webhook: