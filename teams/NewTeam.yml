name: "New team"
team_settings:
  secrets:
    - secret: newteam
agent_options:
controls:
  macos_settings:
    custom_settings: 
      - path: ../lib/macos/configuration-profiles/reedtimmer-profile.mobileconfig
policies:
queries:

